# The main pipeline for ingestion_framework
resources:
  pipelines:
    ingestion_framework_pipeline:
      name: ingestion_framework_pipeline
      target: ingestion_framework_${bundle.environment}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: /Workspace/${workspace.file_path}/src
